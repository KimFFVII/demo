name: Rebase
on:
  pull_request:
    types: [created, labeled]
jobs:
  rebase:
    name: Rebase
    runs-on: ubuntu-latest
    steps:
      - name: Enable auto-merge for Dependabot PRs
        run: gh pr merge --rebase main
